# GlobeNews Live Environment Variables

# Telegram Bot Token — MUST be set for the bot to work
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Iran Alert Bot Token (optional — separate alert bot)
# IRAN_ALERT_BOT_TOKEN=your_alert_bot_token_here
# TELEGRAM_CHAT_ID=your_chat_id_here

# Alerts API Key — required for /api/alerts authentication
# Generate a strong random key, e.g.: openssl rand -hex 32
ALERTS_API_KEY=your_alerts_api_key_here

# Allowed Telegram Chat IDs for alert destinations (comma-separated)
ALLOWED_TELEGRAM_CHAT_IDS=comma_separated_chat_ids_here

# Push Notification VAPID Keys
# Generate keys with: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_public_key_here
VAPID_PRIVATE_KEY=your_private_key_here
VAPID_SUBJECT=mailto:admin@globenews.live

# Optional: Database for storing subscriptions (default: in-memory)
# DATABASE_URL=postgresql://...

# Optional: Redis for pub/sub push (default: in-memory)
# REDIS_URL=redis://localhost:6379
